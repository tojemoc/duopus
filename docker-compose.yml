services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: duopus
      POSTGRES_PASSWORD: duopus
      POSTGRES_DB: duopus
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U duopus -d duopus"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      POSTGRES_URL: ${POSTGRES_URL:-postgresql+psycopg://duopus:duopus@db:5432/duopus}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      VMIX_HOST: ${VMIX_HOST:-host.docker.internal}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8000:8000"

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    depends_on:
      - backend
    ports:
      - "80:80"

volumes:
  pgdata:
